install:
  runtime-versions:
    nodejs: 18
  commands:
    - echo Installing Angular CLI globally
    - npm install -g @angular/cli
    - echo Installing project dependencies
    - npm install
