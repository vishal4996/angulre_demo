version: 0.2
phases:
  install:
    runtime-versions:
      nodejs: 18  # Specify the Node.js version you need
    commands:
      - npm install -g @angular/cli
  pre_build:
    commands:
      - npm install
      - npm run build
